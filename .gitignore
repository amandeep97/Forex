node_modules/
dist/
.env
.env.local
*.local
.claude/

# The black box: written synchronously at shutdown, read and published on the
# next boot, then deleted. Never committed.
vps-bot/.last-shutdown.json
